import React from 'react';
import { type RouteObject } from 'react-router-dom';
import { PlaceholderScreen } from '../placeholder-screen';

export const routes: RouteObject[] = [{ path: '/', element: <PlaceholderScreen /> }];
